trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  imageName: 'shivrajpatil1998/app2'

stages:
- stage: Build
  jobs:
  - job: Build
    steps:
    - checkout: self

    - task: Docker@2
      inputs:
        containerRegistry: 'Dockerserviceconnection'
        repository: $(imageName)
        command: 'buildAndPush'
        Dockerfile: 'app2/Dockerfile'
        tags: |
          latest
          #$(Build.BuildId)

- stage: Deploy
  jobs:
  - job: Deploy
    steps:
    - checkout: self  

    - task: Kubernetes@1
      displayName: 'Create Namespace if not exists'
      inputs:
        connectionType: 'Kubernetes Service Connection'
        kubernetesServiceEndpoint: 'Kubernetes Service Connection'
        command: apply
        useConfigurationFile: true
        configuration: |
          apiVersion: v1
          kind: Namespace
          metadata:
            name: app2

    - task: Kubernetes@1
      displayName: 'Deploy to GKE'
      inputs:
        connectionType: 'Kubernetes Service Connection'
        kubernetesServiceEndpoint: 'Kubernetes Service Connection'
        command: apply
        useConfigurationFile: true
        configuration: |
          $(System.DefaultWorkingDirectory)/app2/k8s/deploy-app2.yaml
          $(System.DefaultWorkingDirectory)/app2/k8s/service-app2.yaml
